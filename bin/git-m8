#!/bin/bash
f() {
  local b=${1:-remotes/origin/develop};
  git for-each-ref --no-merge $b --format='%(refname:short) %(authorname)' 'refs/remotes/origin/developments' \
    | grep Sylvain \
    | sed 's_^origin/\([^[:blank:]]*\).*$_\1_' \
    | sort
}
f $@

